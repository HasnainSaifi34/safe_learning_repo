name: safe_learning
channels:
  - defaults
dependencies:
  - python=3.6
  - pip
  - pip:
      - tensorflow==1.14.0
      - gpflow==1.0.0
      - numpy==1.14.5
      - scipy
      - matplotlib
      - cvxpy
      - notebook
